# {{ ansible_managed }}

{% for node in groups['all'] %}
{{ hostvars[node]['ansible_default_ipv4']['address'] }}	{{ hostvars[node]['ansible_fqdn'] }}	{{ hostvars[node]['ansible_hostname'] }}
{% endfor %}
